{
  "name": "tttn-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "mobile": "npm start --prefix apps/mobile",
    "web": "npm run dev --prefix apps/web",
    "convex:dev": "npx convex dev"
  },
  "dependencies": {
    "convex": "^1.33.1"
  }
}
